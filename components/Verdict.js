const verdictTemplate = (gameStatus) => `
  <div class="game-over">
    <div>
      <h1>Game Over</h1>
      <p class="game-status">${gameStatus}</p>
    </div>
  </div>
`;

export default verdictTemplate;

